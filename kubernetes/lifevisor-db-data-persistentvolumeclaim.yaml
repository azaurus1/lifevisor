apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: lifevisor-db-data
  name: lifevisor-db-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 4Gi
